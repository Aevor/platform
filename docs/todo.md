# Aevor Development TODO

Tracking the progression of Aevor Task 1 (GitHub OAuth login + user registration).
Canonical project docs live in the `docs` repo (`docs/development-state.md`); this file mirrors the tracker for the platform repo.

## Completed

- **Task 1 Part 1 — OAuth login init:** `GET /auth/github/login` generates a random OAuth `state` + random PKCE `code_verifier`, stores both in a short-lived HttpOnly SameSite=Lax cookie, and redirects to GitHub with `code_challenge=S256`; `GenerateState`/`VerifyState` (crypto/rand + `subtle.ConstantTimeCompare`).
- **Task 1 Part 2 — OAuth callback:** `GET /auth/github/callback` verifies `state` (constant-time), clears the cookie, exchanges the single-use code with the `code_verifier` (exchange NEVER retried), maps errors (`invalid_state`, `github_authorization_denied`, `invalid_code`, `github_unavailable`).
- **Task 1 Part 3 — JWT + `/users/me`:** `JWTManager` (HS256 via golang-jwt v5, claims sub/iat/exp(7d)/iss/aud), `RequireAuth` middleware, `GET /users/me`; temporary public routes `POST /users`, `GET /users/:id`, `GET /users/github/:id` removed.
- **Task 1 Part 4 — GitHub profile client:** `internal/github` `GetCurrentUser`, injected HTTP client (10s default timeout), injectable base URL (`WithBaseURL`), User-Agent, Bearer auth, redirects disabled, typed error taxonomy (no token leakage).
- **Task 1 Part 5 — User upsert + encrypted token persistence wiring:** callback encrypts the exchanged token (`users.Encrypt`) then persists via `users.FindOrCreateByGitHubID` (`ON CONFLICT (github_id) DO UPDATE ... RETURNING`); Aevor UUID preserved on re-login, token replaced; `users.Repository` is an interface (concrete `gormRepository`) so service/callback logic is unit-testable with fakes.
- **Task 1 Part 6 — Secure GitHub Access Token Storage (verified 2026-08-15):** AES-256-GCM encrypt/decrypt (random nonce, `base64(nonce || ciphertext)`, tamper/wrong-key rejection), `GitHubAccessToken *string` `json:"-"` column, key from `GITHUB_TOKEN_ENCRYPTION_KEY` env (32-byte hex, fail-fast validation), re-login replaces token while preserving the Aevor UUID, token never in responses/logs/errors/DB-plaintext.
- **Task 1 Part 7 — Aevor JWT Issuance (complete, verified 2026-08-15):**
  - `HandleCallback` signs a JWT (`jwtManager.Issue`, HS256, sub = Aevor user UUID, exp 7d) after a successful upsert and the callback returns `{token, user}` per design D7.
  - Claims: `sub` (Aevor UUID), `iss` (`aevor`), `aud` (`aevor-api`), `iat`, `exp` (7 days). No `jti` (not required by the approved design). No GitHub token, PKCE verifier, OAuth state, or profile data in the JWT.
  - `JWTManager` gained a defensive ≥32-byte secret guard (`ErrInvalidJWTSecret`) and an injectable clock (`WithClock`) for deterministic tests; `pkg/config` already fails fast on a missing/short `JWT_SECRET`.
  - Browser transport = JSON response body only (D7). No cookie.
  - Tests: full JWT matrix (sub/iss/aud/iat/exp, HS256, verify with configured key, wrong key, expired, no sensitive claims, independent timing), config validation, and callback integration (success issues JWT; failed auth / failed persistence / signing failure emit no JWT; JWT payload carries only Aevor identity).
- **Task 1 Part 8 — JWT Authentication Middleware (complete, verified 2026-08-15):**
  - Transport (approved, unchanged from Part 7): `Authorization: Bearer <Aevor JWT>` header. No cookie, no query/body/header-derived identity.
  - `RequireAuth(manager)` refactored: `bearerToken(header)` extraction (exact `Bearer ` prefix + non-empty value) → `manager.Verify` (HS256 whitelist, exp/iss/aud, sub parsed as UUID) → verified `uuid.UUID` placed in Gin context under the typed `UserIDKey`; single uniform `abortUnauthorized` helper returns `401 {"error":"unauthorized"}`. Any failure aborts the request — never silently continues.
  - `GetAuthenticatedUserID(c)` helper added; fails safely (`(uuid.Nil, false)`) when no verified identity is present. Handlers consume identity from context — they never parse JWTs or accept client-supplied identity. `GetMe` now uses the helper.
  - JWT never logged; token never echoed in error responses; error response reveals only `{"error":"unauthorized"}`.
  - Tests (`internal/auth/middleware_test.go`): valid token succeeds; missing/malformed/tampered/expired/wrong-signature/missing-sub/invalid-sub/wrong-issuer/wrong-audience rejected; wrong HMAC alg (HS384/HS512 same secret), `alg=none`, and explicit algorithm-confusion (RS256/ES256 with valid keys) rejected; verified UUID reaches context + helper retrieves it; unauthenticated requests never reach the handler; JWT absent from logs and error bodies; query/`X-User-Id` header cannot override JWT identity; helper fails safely without middleware. Deterministic tamper helper (`tamperToken`) — `-count=20` stable.

- **Task 1 Part 9 — Authenticated `/users/me` endpoint test suite:** committed via PRs #33–#38 (see `docs/development-state.md` for the full list).

## Completed (2026-08-22 — broken-functionality repairs)

- **FIX — OAuth re-login upsert column mismatch (production bug):** `UpsertByGitHubID` referenced a nonexistent column `github_access_token` in the `ON CONFLICT DO UPDATE` clause, while GORM maps `User.GitHubAccessToken` to the column `git_hub_access_token`. First-time logins inserted fine, but every REPEAT login failed with `column excluded.github_access_token does not exist`. NOTE: this fix had previously been reported as done but never existed in any commit (`git log --all -S` confirms); it is now actually fixed.
  - `internal/users/repository.go`: corrected column to `git_hub_access_token`; conflict/assignment columns extracted into package-level vars (`upsertConflictColumns`, `upsertAssignmentColumns`).
  - `internal/users/model.go`: explicit `gorm:"column:git_hub_access_token"` tag pins the mapping (no physical schema change; AutoMigrate no-op).
  - New `internal/users/repository_upsert_test.go`: schema-consistency regression tests that validate every upsert-referenced column against the model's parsed GORM mapping (verified to FAIL against the old buggy code).
  - New `internal/users/repository_integration_test.go`: opt-in real-Postgres integration test (`AEVOR_TEST_DATABASE_DSN`) covering insert + conflict-update paths, UUID preservation, token rotation; skipped by default so unit runs stay hermetic. PASSES against live PostgreSQL 16 on localhost:5432.
- **FIX — time-bomb JWT tests:** `TestJWT_ContainsCorrectSubject/ContainsCorrectIssuedAt/ContainsCorrectExpiration/IndependentTokensHaveIndependentTiming` issue tokens at a fixed 2026-08-15 instant (7-day TTL) and parsed them with live wall-clock validation — permanently failing once 2026-08-22 arrived. `parsedClaims` now parses with `jwt.WithoutClaimsValidation()` (claim inspection only); validity semantics remain covered by `manager.Verify` and middleware tests. Suite green again.
- **REVIEW (2026-08-23) — token non-exposure test was ineffective:** `TestUserModel_TokenColumnNeverSerialized` asserted against GORM's `TagSettings["JSON"]`, but `TagSettings` only parses the `gorm:` tag (verified against gorm v1.31.1 source), so the test passed even with `json:"-"` deleted (proven by mutation test). It now asserts `field.Tag.Get("json") == "-"` and is proven to FAIL when the tag is removed. Full re-verification after the fix: build/vet/`go test -count=1`/`go test -race -count=1` all pass; integration test passes against live PostgreSQL (5432) with fixture cleanup confirmed (0 residual rows); live server verified `/health` 200, `/auth/github/login` 302 with PKCE S256 + state cookie, callback without state → `invalid_state`, `/users/me` → uniform `401`; startup AutoMigrate left the physical schema unchanged (`git_hub_access_token` intact, no migration needed).

## Completed (2026-08-23 — Task 2a GitHub repository discovery)

- **Task 2a — `GET /repositories` (complete, verified 2026-08-23):** authenticated endpoint returning the current Aevor user's GitHub repositories through their stored encrypted access token. Discovery only — no persistence, no caching, no AI, no frontend changes.
  - Files: `internal/github/client.go` (+`Repository`/`RepositoryOwner` types, `ListUserRepositories`, `hasNextPage`), `internal/users/service.go` (+`DecryptedGitHubToken`, `ErrGitHubTokenMissing`), NEW package `internal/repositories/{service,dto,handler}.go`, `cmd/server/main.go` (route wiring).
  - Architecture preserved: Handler → Service → GitHub Client → GitHub API; identity solely from `RequireAuth` JWT (`GetAuthenticatedUserID`) — query params/headers can never select another user's credentials (tested).
  - Contract: `GET /repositories?page=&per_page=` (Bearer Aevor JWT). Defaults page=1, per_page=30, per_page clamped to 100, invalid values → 400 `invalid_pagination`. Response `{repositories:[{id,name,full_name,description,private,default_branch,owner_login,html_url,clone_url,api_url}],page,per_page,has_more}`. GitHub call uses GET `/user/repos?sort=full_name&direction=asc` (deterministic order), pagination via Link header (`has_more`).
  - Error surface: 401 `unauthorized` (no/garbage JWT); 404 `user_not_found`; 403 `github_token_missing`; 401 `github_token_invalid` (GitHub rejects stored token — remedy is re-login); 429 `github_rate_limited`; 500 `github_unavailable` (GitHub down/malformed payload/other API errors); 500 `internal` (decryption failure etc., no crypto detail leaked).
  - Tests: `client_repositories_test.go` (request shape incl. deterministic sort + Bearer + API version, mapping, empty list, Link-header has_more true/false, status taxonomy, network failure, malformed/negative-id/blank-full-name rejection, token never in errors), `service_token_test.go` (decrypt round-trip, user-not-found, nil/blank/empty token, corrupt ciphertext, wrong key, short key), `repositories/handler_test.go` (unauthenticated 401 with zero GitHub contact, success mapping + pagination echo, token plaintext AND ciphertext absent from response body, per_page clamp to 100 forwarded, empty array not null, invalid pagination matrix, user_not_found/token_missing/decrypt-failure/GitHub-failure mappings, identity-from-JWT-only vs `?user_id=`, request-context propagation).
  - Verified live (local server + real DB): `/health` 200; `/repositories` unauthenticated and garbage-bearer → uniform 401; fixture user with NULL token → 403 `github_token_missing`; fixture user with encrypted dummy token → GitHub rejects → 401 `github_token_invalid` (full chain proven: JWT → user lookup → decrypt → GitHub call → error map); fixture rows cleaned (0 residue).
  - Deferred finding (pre-existing): `pkg/database/postgres.go` uses GORM's default logger, so SQL errors log full statements including bound ciphertext — tighten logging as a separate task.

## Completed (2026-08-23 — Task 2b repository selection + persisted repository context)

- **Task 2b (complete, verified 2026-08-23):** authenticated selection of a GitHub repository, verified accessible via the user's own stored encrypted token BEFORE any persistence; per-user retrieval; owner-only deselection. No cloning, no indexing, no AI.
  - Files: NEW `internal/repositories/model.go` (`SelectedRepository`, table `selected_repositories`, unique index `idx_selected_repositories_user_repo` on `(user_id, github_repository_id)`, UUID PK via `BeforeCreate`, NO token column), `migration.go` (`AutoMigrate` wired in `main.go`), `repository.go` (`Store` interface {UpsertSelected/ListByUserID/DeleteByUserAndID} + `gormStore`; upsert = `ON CONFLICT (user_id, github_repository_id) DO UPDATE` with `RETURNING`; columns pinned in package vars), MODIFIED `internal/github/client.go` (+`GetRepository` GET `/repositories/{id}`, sentinel `ErrRepositoryNotFound` for 404), `internal/repositories/service.go` (+`store Store` dependency, `SelectForUser`, `ListSelected`, `RemoveSelected`, `ErrSelectedNotFound`), `dto.go`, `handler.go`, `cmd/server/main.go`.
  - Contract (all behind Bearer Aevor JWT):
    - `POST /repositories` body `{"github_repository_id":<int>}` → 200 with the persisted record `{id,github_repository_id,name,full_name,owner_login,private,default_branch,html_url,created_at,updated_at}`. Flow: decrypt user's token → GitHub GET `/repositories/{id}` → 404 → **404 `repository_not_found` and NOTHING is persisted** → success persists authoritative GitHub metadata. Re-selecting the same repo UPDATES the existing row in place (unique index), never duplicates.
    - `GET /repositories/selected` → 200 `{"repositories":[...]}` scoped strictly to the authenticated user.
    - `DELETE /repositories/:id` (Aevor record UUID) → 204 on the owner's own record; 404 `repository_not_found` for foreign/unknown records (existence of other users' records never revealed); 400 `invalid_request` for malformed UUIDs.
  - Error surface unchanged from 2a plus: 404 `repository_not_found`. Identity solely from JWT; a request can only ever select/list/delete rows bound to its own verified `sub`.
  - Tests: `client_repository_test.go` (path/bearer shape, mapping, 404→sentinel, status taxonomy incl. network failure, malformed/negative-id/blank-full-name rejection, token never in errors); `selection_test.go` (schema-consistency regression test validating every upsert conflict/assignment column against the parsed model mapping; full handler matrix: unauth 401 with zero GitHub contact, invalid-body matrix, success path asserting the authenticated user's DECRYPTED token reaches the mock GitHub and authoritative metadata is stored bound to the user's UUID, inaccessible-repo 404 with nothing persisted, GitHub failure mappings, missing-token 403, corrupt-ciphertext 500 `internal`, list isolation between two users sharing one github_repository_id, delete owned/foreign/unknown/malformed/unauth matrix, fake-store duplicate-selection refresh semantics); `repository_integration_test.go` (opt-in real-Postgres: insert→conflict-update preserves the same Aevor UUID while refreshing metadata, cross-user independence for the same repo, scoped delete, foreign-delete rejected). All suites green: build/vet/test/`-race`; integration PASSES against live PostgreSQL 16 (localhost:5432).
  - Verified live (local server + real DB): startup migration created `selected_repositories` with the exact schema + unique index; `/health` 200; POST unauth → 401; fixture user with encrypted dummy token → real GitHub rejects → 401 `github_token_invalid` with **0 rows persisted** (access-verification gate proven live); seeded row visible ONLY to its owner; foreign DELETE → 404; owner DELETE → 204; post-delete list empty; all fixtures cleaned (0 residual users/repositories rows); temp helper removed.

- **Task 1 close-out:**
  - DONE: repairs implemented, reviewed, tested, and committed on branch `fix/users-upsert-token-column` (`098dd4d` fix+regression tests, `a8b4479` jwt deflake, docs commit).
  - BLOCKED (human step): manual end-to-end OAuth re-login in a real browser — exercises the fixed conflict path live (DB level already proven by the integration test).
  - TODO (Sanjeev): push the branch, open PR, review, approve, merge.

## Completed (2026-08-23 — Task 2c issue ingestion)

- **Task 2c — `POST /repositories/:id/issues/sync` (complete, verified 2026-08-23):** deterministic, AI-free synchronization of GitHub ISSUE METADATA for one of the authenticated user's selected repositories. No cloning, no indexing, no background workers, no queues.
  - Database: NEW `RepositoryIssue` model → `repository_issues` table (uuid PK; unique index `idx_repository_issues_selected_github` on `(selected_repository_id, github_issue_id)`; number/title/state/author_login/html_url; GitHub timestamps under `github_*` prefix to avoid confusion with Aevor row timestamps; `synced_at` bookkeeping). Issue BODIES deliberately not persisted yet (documented limitation; additive later). Wired into the existing per-package AutoMigrate (`repositories.Migrate`).
  - GitHub client (extended the EXISTING client, no duplicates): `ListRepositoryIssues` → GET `/repos/{owner}/{repo}/issues?state=all&sort=updated&direction=desc&page=&per_page=`; reuses HTTP client, headers, API version, response-size guard (1MB), error taxonomy, Link-header pagination. Pull-request entries included by GitHub's issues endpoint are FILTERED OUT before validation; per-issue validation rejects negative ids/numbers, blank title/author/url, missing created_at, unsupported states (`ErrInvalidResponse`). 404 maps to the shared sentinel `ErrRepositoryNotFound`.
  - Sync semantics: bounded deterministic pagination — up to 10 pages × 100 issues (`syncPerPage`/`syncMaxPages`), most-recently-updated first; all-or-nothing persistence in ONE transaction via batched `ON CONFLICT (selected_repository_id, github_issue_id) DO UPDATE` (batches of 250). Re-sync updates authoritative mutable metadata in place (title/state/closed_at/updated_at/synced_at) and never duplicates. Store resets surrogate UUIDs on input so reused structs cannot collide on the primary key.
  - Security/ownership: identity solely from JWT; ownership resolved FIRST via `Store.FindByUserAndID(userID, id)` — unknown and foreign contexts map identically to 404 `repository_not_found` BEFORE any token work or GitHub contact; owner/name sent to GitHub come from OUR stored authoritative selection metadata (client can never redirect the sync); user's encrypted token decrypted server-side per existing `DecryptedGitHubToken`; token material asserted absent from responses/errors/tests.
  - Response DTO: exactly `{"repository_id":"<aevor uuid>","synced":<count>}` — nothing else.
  - Error surface: 401 `unauthorized`; 400 `invalid_request` (malformed uuid); 404 `repository_not_found` (unknown OR foreign context, or repository deleted/renamed upstream); 404 `user_not_found`; 403 `github_token_missing`; 401 `github_token_invalid`; 429 `github_rate_limited`; 500 `github_unavailable`; 500 `internal` (persistence/crypto failures, detail suppressed).
  - Tests: `internal/github/client_issues_test.go` (endpoint/query/header shape incl. PathEscape, mapping, PR filtering, Link pagination true/false, full status taxonomy, network failure, malformed-response matrix incl. object-instead-of-array, >1MB oversized-response rejection, token never in errors); `internal/repositories/issues_sync_test.go` (`TestUpsertIssues_ColumnsMatchModelSchema` schema-consistency regression test + handler matrix: unauth with zero GitHub contact, malformed uuid, unknown/foreign uniform 404 with zero GitHub contact, missing-token 403, corrupt-ciphertext 500 internal, GitHub failure mappings incl. renamed-repo 404, success path asserting decrypted-user-token-to-mock + authoritative-metadata binding + exact DTO shape + no extra keys + plain/ciphertext absence, bounded pagination = exactly maxPages requests, repeated sync no-duplicate + metadata refresh, cross-user isolation sharing one repository, cancelled-context persists nothing); `internal/repositories/repository_issues_integration_test.go` (real Postgres: migration, insert, duplicate upsert preserves uuid while refreshing title/state/closed_at, unique index, same github issue independent per context, FindByUserAndID ownership isolation; self-healing cleanup). Full gate green: gofmt/build/vet/test/`-race`; integration PASSES against live PostgreSQL 16 (localhost:5432).
  - Config addition: optional `GITHUB_API_BASE_URL` env override (empty = production default; documented in `.env.example`) so the REAL server can be pointed at a LOCAL mock GitHub for end-to-end verification.
  - Verified live (local server :8081 + mock GitHub :8099 + real DB): startup created `repository_issues` with exact schema + unique index; `/health` 200; unauth sync → 401 before uuid parsing; foreign sync → 404 with zero mock contact; corrupt ciphertext → 500 `internal`; rejected token → mock 401 → `github_token_invalid`; happy path → 200 `{repository_id, synced:3}` with PR filtered and page-1→page-2 Link pagination followed using the OWNER's decrypted bearer; SECOND sync still exactly 3 rows AND issue title refreshed in place ("Renamed by second live sync"); token plaintext absent everywhere; ALL fixture rows cleaned (0 residual users/selected_repositories/repository_issues rows); temp helpers deleted, servers stopped.

## Completed (2026-08-23 — Task 2d pull-request ingestion)

- **Task 2d — `POST /repositories/:id/pull-requests/sync` (complete, verified 2026-08-23):** deterministic, AI-free synchronization of GitHub PULL REQUEST METADATA for one of the authenticated user's selected repositories, mirroring Task 2c's architecture exactly. No cloning, no indexing, no background workers, no queues.
  - Database: NEW `RepositoryPullRequest` model → `repository_pull_requests` table (uuid PK; unique index `idx_repository_pull_requests_selected_github` on `(selected_repository_id, github_pull_request_id)`; number/title/state/author_login/html_url/head_ref/base_ref/draft/merged; GitHub timestamps under `github_*` prefix incl. `github_merged_at`; `synced_at` bookkeeping). PR BODIES/diffs deliberately not persisted (documented limitation). Wired into the existing per-package AutoMigrate (`repositories.Migrate` now migrates all three models).
  - GitHub client (same shared client, no duplicates): `ListRepositoryPullRequests` → GET `/repos/{owner}/{repo}/pulls?state=all&sort=updated&direction=desc&page=&per_page=` with `url.PathEscape`'d owner/repo; reuses HTTP client, headers, API version, response-size guard (1MB), error taxonomy, Link-header pagination (the pulls endpoint returns ONLY PRs — no filtering needed). Per-item validation rejects negative ids/numbers, blank title/author/url/head-ref/base-ref, missing created_at, unsupported states (`ErrInvalidResponse`). 404 maps to the shared sentinel `ErrRepositoryNotFound`.
  - Sync semantics: identical bounded pagination (`collectBoundedPages`, up to 10 × 100), all-or-nothing transactional persistence via batched `ON CONFLICT (selected_repository_id, github_pull_request_id) DO UPDATE` (batches of 250); re-sync refreshes mutable metadata in place (title/state/branches/draft/merged/closed_at/merged_at/synced_at) and never duplicates.
  - NEW hardening (found during live verification, applies to issues too): `sort=updated` pagination is not snapshot-stable — an item edited mid-sync can be served on two pages, and a multi-row ON CONFLICT INSERT containing a duplicate key fails with SQLSTATE 21000 ("cannot affect row a second time"). Added generic `dedupByGitHubID` (keeps first occurrence) applied to BOTH issue and pull-request batches before persistence; regression test `TestPRSync_OverlappingPagesDeduplicated`.
  - Security/ownership: byte-for-byte the Task 2c contract — identity solely from JWT; ownership FIRST via `FindByUserAndID` (unknown ≡ foreign → 404 `repository_not_found`, zero GitHub contact, BEFORE token resolution); owner/name from OUR stored authoritative metadata; decrypted user token used as bearer only against GitHub; token material absent from responses/errors/logs/tests.
  - Response DTO: exactly `{"repository_id":"<aevor uuid>","synced":<count>}`.
  - Error surface: identical to Task 2c (401 `unauthorized`; 400 `invalid_request`; 404 `repository_not_found`; 404 `user_not_found`; 403 `github_token_missing`; 401 `github_token_invalid`; 429 `github_rate_limited`; 500 `github_unavailable`; 500 `internal`).
  - Tests: `internal/github/client_pull_requests_test.go` (endpoint/query/header shape, full mapping incl. draft/head/base/merged_at, Link pagination true/false, status taxonomy incl. 404→ErrRepositoryNotFound, network failure, malformed matrix incl. blank head/base ref and object-instead-of-array, >1MB rejection, token never in errors); `internal/repositories/pull_requests_sync_test.go` (`TestUpsertPullRequests_ColumnsMatchModelSchema` + handler matrix mirroring 2c: unauth/malformed-uuid/unknown≡foreign-404-with-zero-contact/missing-token-403/corrupt-ciphertext-500/GitHub-failure-mappings/success-DTO-and-token-absence/bounded-pagination-exactly-maxPages/repeated-sync-no-duplicates-with-metadata-refresh/cross-user-isolation/cancelled-context + NEW dedup regression test); `internal/repositories/repository_pull_requests_integration_test.go` (real Postgres: migration, insert→conflict-upsert preserves uuid while refreshing title/state/branches/draft/merged/merged_at, same github PR independent per context, ownership isolation; self-healing cleanup). Full gate green: gofmt/build/vet/test/`-race`; integration PASSES against live PostgreSQL 16.
  - Verified live (server :8081 + mock GitHub :8099 serving /pulls + real DB): startup created `repository_pull_requests` with exact schema + unique index name; `/health` 200; unauth → 401; foreign → 404 zero mock contact; no-token-user on foreign context → 404 (ownership precedes token resolution); garbage-token plaintext → GitHub 401 → `github_token_invalid`; happy sync → 200 `{synced:3}` following Link page1→page2 with owner's bearer; SECOND sync → 200 `{synced:2}` after upstream retitle+merge, SAME 3 rows with PR 7001 refreshed in place (title/state/merged updated, uuid preserved); token plaintext nowhere; ALL fixture rows cleaned (0 residual rows across all four tables); temp helpers deleted; servers stopped.

## Completed (2026-08-23 — Task 2e commit ingestion)

- **Task 2e — `POST /repositories/:id/commits/sync` (complete, verified 2026-08-23):** deterministic, AI-free synchronization of Git COMMIT METADATA for one of the authenticated user's selected repositories, mirroring Tasks 2c/2d exactly. No AI, no cloning, no code parsing, no indexing, no workers, no queues.
  - Database: NEW `RepositoryCommit` model → `repository_commits` table (uuid PK; unique index `idx_repository_commits_selected_sha` on `(selected_repository_id, github_commit_sha varchar(40))`; message (text ONLY — never diffs/file contents); author_name/author_email/author_login/committer_name; html_url; `github_authored_at` + `github_committed_at` under the Github* prefix; `synced_at` bookkeeping). The SHA is the natural in-repository identifier and is normalized to lowercase before persistence. GitHub's linked account is OPTIONAL upstream (author email matching no GitHub account → empty author_login) — deliberately unlike issues/PRs where login is mandatory. Wired into the existing per-package AutoMigrate (`repositories.Migrate` now migrates all FOUR models).
  - GitHub client (same shared client): `ListRepositoryCommits` → GET `/repos/{owner}/{repo}/commits?page=&per_page=` (GitHub's authoritative newest-first default order); reuses HTTP client, headers, API version, response-size guard (1MB), error taxonomy, Link pagination. Per-item validation rejects malformed entries wholesale (`ErrInvalidResponse`): non-hex/non-40-char SHAs, blank messages/URLs/author names, missing authored or committed dates. NOTE: GitHub nests `message` inside the `commit` object — an initial implementation bug decoded it from the top level and was caught by tests. 404 maps to shared `ErrRepositoryNotFound`.
  - Sync semantics: identical bounded pagination via generic `collectBoundedPages` (≤10 × 100); all-or-nothing transactional persistence via batched `ON CONFLICT (selected_repository_id, github_commit_sha) DO UPDATE` (batches of 250); conflict columns pinned in `commitUpsertAssignmentColumns` guarded by `TestUpsertCommits_ColumnsMatchModelSchema`. A SHA's Git content is immutable but GitHub-side metadata can change (linked account appears) — refresh includes author_login + bookkeeping. The generic dedup hardening was renamed `dedupByKey[T, K comparable]` and now also collapses duplicate SHAs within one sync batch.
  - Security/ownership: byte-for-byte the 2c/2d contract — identity solely from JWT; ownership FIRST via `FindByUserAndID` (unknown ≡ foreign → 404 `repository_not_found`, zero GitHub contact, BEFORE token resolution); owner/name from stored authoritative metadata; decrypted user token server-side only; response exactly `{repository_id, synced}`; error surface unchanged.
  - Tests: `internal/github/client_commits_test.go` (endpoint/query/header shape, mapping incl. null-account commits, SHA lowercase normalization, Link pagination, full status taxonomy, network failure, malformed matrix incl. short/non-hex SHAs + missing dates, >1MB rejection, token never in errors); `internal/repositories/commits_sync_test.go` (schema-consistency regression + handler matrix mirroring 2c/2d: unauth/malformed-uuid/unknown≡foreign-404-with-zero-contact/missing-token-403/corrupt-ciphertext-500/GitHub-failure-mappings/success-DTO-and-token-absence/bounded-pagination-exactly-maxPages/repeated-sync-no-duplicates-with-linked-account-refresh/cross-user-isolation-on-same-repository/cancelled-context); `internal/repositories/repository_commits_integration_test.go` (real Postgres: migration, insert→conflict-upsert preserves uuid while refreshing linked account, same SHA independent per context, ownership isolation; self-healing cleanup).
  - Verified live (server :8081 + mock GitHub :8099 serving /commits + real DB): startup created `repository_commits` with exact schema + index name; `/health` 200; unauth 401; foreign 404 zero mock contact; rejected token 401 `github_token_invalid`; happy sync `{synced:3}` following Link page1→page2 with owner's bearer; second sync `{synced:3}` SAME rows with newly-linked author login refreshed in place; zero token leakage; ALL fixture rows cleaned (0 residual rows across all FIVE tables); temp helpers deleted; servers stopped.

## Completed (2026-08-23 — Task 3a secure repository access + workspace cloning)

- **Task 3a — `POST /repositories/:id/clone` (complete, verified 2026-08-23):** ensures one of the authenticated user's selected repositories exists as a private local Git workspace under a server-controlled root. First deterministic step toward codebase-level evidence. No AI, no code parsing/indexing yet.
  - NEW dependency: go-git v5.16.4 — pure-Go Git implementation, deliberately chosen over shelling out to the `git` binary (no hook execution, no repository-config execution, no command-injection surface).
  - NEW package `internal/workspace`: `Manager` (root validated at construction; per-workspace dirs keyed by SelectedRepository UUID; `LockFor` serializes concurrent workspace operations; stateless persistence — `.git` present AND `HEAD` resolvable = READY; `Reset` wipes partial/corrupted dirs before retry; `Discard` removes them on failure) and `GoGitCloner` (shallow `depth=1` + `SingleBranch` as resource guard; 5-minute default deadline).
  - Credential handling: plaintext token lives in memory ONLY for the operation and is passed exclusively via go-git per-operation HTTP basic auth (`x-access-token`). The clone URL persisted into each workspace's `.git/config` stays credential-free (test-asserted). Tokens never logged/stored/returned.
  - Clone-URL trust boundary: URL taken ONLY from the authoritative `GET /repositories/{id}` response; validator enforces https + host allowlist (`CLONE_ALLOWED_HOSTS`, default `github.com`); rejects http/ssh/scp/host-lookalikes/userinfo-embedded credentials; `file://` requires explicit `CLONE_ALLOW_FILE_URLS=true` (local dev/integration only).
  - Config: REQUIRED `WORKSPACE_ROOT` (fail-fast at startup), optional `CLONE_ALLOWED_HOSTS`, optional `CLONE_ALLOW_FILE_URLS`; `.env.example` updated.
  - Flow: ownership FIRST → decrypted token → authoritative GetRepository → URL policy → lock → idempotent Ready short-circuit → Reset → bounded clone → post-clone verification; ANY failure Discards partials. Errors: timeout→504 `clone_timeout`, Git auth rejection→401 `github_token_invalid`, other clone failures→500 `internal` (causes server-log-only). Response exactly `{repository_id, status:"ready"}`.
  - Tests: `internal/workspace/cloner_test.go` (validator matrix incl. userinfo injection, file opt-in, unreachable-URL classification); `internal/repositories/clone_test.go` (REAL go-git clone from a locally built file:// repo proving working-tree files land + `.git/config` stays token-free; idempotent second call = zero cloner invocations; corrupted-workspace repair; failure discards; auth-reject + timeout mappings incl. elapsed-time gate; policy-violation matrix with zero clones attempted; foreign ≡ unknown 404; missing token; vanished upstream repo; handler contract incl. uniform 401 + identifier-only success + opaque foreign 404). Fixtures migrated to shared `newTestService`.
  - Verified live (server :8088 + mock GitHub serving authoritative payload with file:// clone_url + real PostgreSQL): `/health` 200; unauth 401; foreign 404 `repository_not_found`; happy clone 200 ready with real workspace on disk (`README.md` + `.git`), ZERO occurrences of the plaintext token under `.git/config` or logs; second call idempotent; cleanup left 0 residual rows; temp helper removed.

## Completed (2026-08-23 — Task 3b repository codebase discovery)

- **Task 3b — `POST /repositories/:id/discover` (complete, verified 2026-08-23):** READ-ONLY inspection of an already-cloned Aevor workspace for one of the authenticated user's selected repositories. Metadata only — no AI, no embeddings, no vector DB, no code analysis, no source content returned.
  - NEW package `internal/discovery` (pure Go, stateless, concurrency-safe): metadata-only walker (`filepath.WalkDir`, Lstat semantics) counting files/directories; programming-language detection by file extension ONLY (md/json/yaml/toml excluded so docs/config cannot drown out code signals); important project/configuration markers (README/LICENSE/Makefile/Dockerfile/docker-compose/package.json/go.mod/Cargo.toml/pom.xml/build.gradle/requirements.txt/pyproject.toml/setup.py/composer.json/Gemfile/tsconfig.json/`.github/workflows/*.yml|yaml`) reported as workspace-RELATIVE paths capped at 50.
  - SECURITY by construction: nothing executed; file CONTENTS never read into memory; symlinks NEVER followed (linked files/dirs counted skipped + pruned — symlink escape can neither leak server files nor leave the workspace); only aggregates + relative paths cross the API boundary; absolute paths impossible in responses.
  - IGNORE RULES: `.git`, `node_modules`, `vendor`, `target`, `dist`, `build`, `out`, `.next`, `coverage`, `__pycache__`, `venv/.venv`, `.gradle`, `.idea` pruned from results but NEVER deleted from disk. Configurable via `discovery.Options{MaxFiles, MaxFileSize, IgnoredDirs}` with package defaults; deliberately not env-wired yet.
  - LIMITS: ≤20,000 files per run (beyond → `truncated:true`), files >1 MiB excluded and tallied as skipped, traversal bounded by request context.
  - PERSISTENCE DECISION: no new table — recomputed on demand (filesystem is truth, walking is cheap); source files stay out of PostgreSQL; persisted manifest deferred until a real consumer exists.
  - Service flow mirrors CloneRepository: ownership FIRST → readiness gate (missing/corrupted workspace → 409 `workspace_not_ready`, remedy = clone first) → walk. No GitHub contact, no token use. New errors: 409 `workspace_not_ready`, 504 `discovery_timeout`.
  - Response: `{repository_id, files, directories, languages:{}, important_files?:[], truncated, status:"discovered"}`.
  - Tests: `internal/discovery/discovery_test.go` (nested counts/languages/markers incl. binary exclusion; ignored dirs preserved on disk; symlink file+dir escape; large-file skip; truncation; empty workspace; missing root; cancelled context; language table); `internal/repositories/discover_test.go` (unauth 401 uniform; malformed uuid 400; foreign ≡ unknown opaque 404; missing/corrupted workspace 409; happy path vs a REAL cloned workspace with exact counts + zero leakage of tmp paths/source markers/symlinked server secrets).
  - Verified 2026-08-23: gofmt clean; build/vet green; `go test ./... -count=1` AND `go test -race ./... -count=1` ALL green across all seven packages.

## Completed (2026-08-24 — Task 3c codebase filtering + safe file selection)

- **Task 3c — `POST /repositories/:id/filter` (complete, verified 2026-08-24):** deterministic, read-only selection of which files in an already-cloned workspace are suitable for future codebase analysis and AI processing. Explicit per-file DECISIONS (path/size/extension/language/category/included/reason) plus aggregate counts. No AI, no embeddings, no code analysis beyond classification, no source content returned.
  - NEW package `internal/filtering`: policy tables (`filter.go`) + one bounded traversal (`service.go`). REUSES Task 3a/3b instead of duplicating: workspace containment comes from `workspace.Manager.Dir/Ready`, the ignored-directory set is `discovery.DefaultIgnoredDirs()` (single source of truth), and language labels come from the now-exported `discovery.LanguageForExtension` (behavioral no-op rename for discovery).
  - POLICY (deterministic, fixed order per candidate): secret risk (.env*, id_rsa/.pem/.key/.jks/credentials.json/.npmrc/...) → per-file size limit (`too_large`, never silently truncated) → known binary families (archives/images/video/audio/db/compiled/fonts/opaque docs → `binary`) → generated artifacts (lockfiles incl. package-lock.json/yarn.lock/Cargo.lock/go.sum, .min.js/.min.css bundles, source maps, snapshots → `generated`) → junk text (.log/.bak/.swp/.svg... → `ignored_extension`) → config by name/prefix (Makefile/Dockerfile/go.mod/Cargo.toml/pyproject.toml/requirements.txt/pom.xml/build.gradle*/CMakeLists.txt/.gitignore/.editorconfig/dotfile configs) → docs by name/prefix (README*/LICENSE*/CONTRIBUTING*/CHANGELOG*...) → docs/config by extension (.md/.rst/.txt; .json/.yaml/.toml/.xml...) → source by shared extension tables (+ small labeled additions: .sql/.proto/.graphql/.css/.html/...) → else `unsupported`.
  - IMPORTANT FILES kept: README.md, LICENSE, CONTRIBUTING.md, package.json, go.mod, Cargo.toml, pyproject.toml, requirements.txt, pom.xml, build.gradle, Dockerfile, Makefile, `.github/workflows/*` all included (name rules beat generic text-extension rules; boundary rule prevents e.g. `install.sh` matching an "install" doc prefix). Documentation/configuration/source remain separate categories.
  - LIMITS (env-configurable, fail-fast on invalid values): `FILTER_MAX_FILE_SIZE` (default 1 MiB, excluded as `too_large`), `FILTER_MAX_TOTAL_BYTES` (default 32 MiB running budget over SELECTED bytes; a candidate that would overflow is excluded `total_size_limit`; a later smaller file that still fits stays selectable — deterministic because walk order is deterministic), `FILTER_MAX_FILES` (default 5000 included files; overflow excluded `file_count_limit`). Per-file detail list capped at 1000 entries sorted by path with `files_truncated` flag; counts always exact. Wired via `pkg/config` + `.env.example`.
  - SECURITY: nothing executed, file contents never read (decisions from Lstat metadata only), symlinks never followed (skipped+pruned, escape impossible), only repository-relative slash paths leave the package, hostile path shapes fail CLOSED (defense-in-depth validator), ignored dirs pruned but never deleted, secrets conservatively EXCLUDED (incl. `.env.example` — documented), no absolute paths in responses, ownership FIRST (`FindByUserAndID`, unknown ≡ foreign → uniform 404) then readiness gate (409 `workspace_not_ready`) then filter (504 `filter_timeout` on deadline).
  - GITIGNORE DECISION: deliberately NOT integrated — Git ignore rules express build hygiene, not analysis suitability, and would make selection depend on patterns outside our control. Policy stays explicit/deterministic; independence is TESTED (`TestFilter_GitignoreDoesNotInfluencePolicy`). Revisit only with a demonstrated need.
  - PERSISTENCE DECISION: none — recomputed on demand like discovery; no source code in PostgreSQL; no new tables.
  - Response: `{repository_id, total_files, included_files, excluded_files, total_selected_bytes, languages:{}, exclusion_summary:{reason:count}, files:[decisions], files_truncated, ignored_directories, symlinks_skipped, truncated, status:"filtered"}`.
  - Tests: `internal/filtering/filter_test.go` (path validator matrix incl. traversal/null-byte; classify matrix for every reason incl. precedence order, case-insensitivity, boundary prefixes); `internal/filtering/service_test.go` (selection basics, nested sources, ignored dirs preserved on disk, oversized→too_large, symlink file+dir escape, gitignore independence, max-files truncation, size-budget semantics, byte-for-byte determinism across runs, relative-path-only/no-content-leak serialization, empty/missing root, cancelled context); `internal/repositories/filter_test.go` (unauth uniform 401, malformed uuid 400, foreign ≡ unknown opaque 404, missing/corrupted workspace 409, happy path vs a REAL cloned workspace with exact counts/reasons/languages + zero leakage of tmp paths/source markers/env values/symlinked server secrets, node_modules untouched, deterministic identical bodies).
  - Verified 2026-08-24: `gofmt -l .` clean; `go build ./...`, `go vet ./...`, `go test ./... -count=1`, `go test -race ./... -count=1` ALL green across all eight packages.

## Completed (2026-08-24 — Task 3d codebase content extraction)

- **Task 3d — `POST /repositories/:id/extract` (complete, verified 2026-08-24):** bounded, deterministic extraction of the TEXTUAL CONTENT of exactly the files Task 3c selected, into an internal in-memory representation (path/language/extension/size/content/SHA-256) for future pipeline stages. No AI, no chunking yet, no persistence of source code.
  - NEW package `internal/extraction`: `Service.Extract(ctx, root)` composes the SAME configured `filtering.Service` and reads ONLY decisions marked included — binary/secret/generated/junk/unsupported files never reach the read path by construction. `ExtractDecisions(ctx, root, decisions)` is the core, so hostile/synthetic decision lists are directly testable.
  - CONTENT MODEL: `File{Path (repository-relative slash path), Language, Extension, Size, Content (EXACT bytes as string — line endings AND BOM preserved), ContentHash (lowercase hex SHA-256 of those exact bytes)}`. Result carries exact counts (`total_candidates/extracted_count/extracted_bytes/skipped{reason:count}/skipped_count/complete`) plus the COMPLETE Files list — uncapped internally because total size is already bounded by the shared filtering budget; only the HTTP DTO caps at 1000 entries (`files_truncated`). The hash is a change/duplicate detector only, NEVER an authorization mechanism.
  - PATH SAFETY: every read goes through Go's `os.Root` over the server-controlled workspace — traversal rejected up front via the shared `filtering.ValidRelativePath`, symlink escapes refused by kernel-assisted root resolution; a leaf that became a symlink after filtering is SKIPPED (`symlink`), never followed; non-regular files skipped (`not_regular_file`); hostile paths fail CLOSED with `ErrUnsafePath`.
  - DECODING POLICY (documented decision): strict UTF-8 or skip (`invalid_utf8`) — NO lossy U+FFFD replacement; invalid content stays intact on disk. Binary-by-extension files are excluded pre-read by filtering; binary content inside a text-extension file is caught by the UTF-8 check.
  - LIMITS: selection budgets inherited from the shared filterer (`FILTER_MAX_FILE_SIZE`/`FILTER_MAX_TOTAL_BYTES`/`FILTER_MAX_FILES`); per-file size RE-verified at open time (`Stat` + `io.LimitReader(max+1)`) closing the filter→read race — growth past the cap → skip `too_large`, never silently truncated; whole run bounded by `extraction.DefaultTimeout` (1 min). No new env vars introduced deliberately.
  - SKIP VOCABULARY (extends filtering's): `not_selected`, `too_large`, `symlink`, `not_regular_file`, `invalid_utf8`, `duplicate_path` (first occurrence wins under lexical ordering).
  - API BOUNDARY DECISION: extracted CONTENTS are internal-only — the endpoint returns METADATA ONLY (`path/size/extension/language/content_hash`; there is no content field in the response DTO at all), following the existing repository-API conventions without creating a source-dump endpoint.
  - SECURITY: nothing executed/interpreted/installed; contents never logged (service logs counts/bytes/duration/user/repo IDs only; error wraps carry RELATIVE paths only); ownership FIRST (`FindByUserAndID`, unknown ≡ foreign → uniform 404) then readiness gate (409 `workspace_not_ready`) then extract (504 `extract_timeout` maps BOTH filtering and extraction deadline sentinels); ignored dirs untouched on disk; no absolute paths in responses.
  - Tests: `internal/extraction/extraction_test.go` — all 18 required cases: normal UTF-8 source, multiline + CRLF preservation, empty file (known SHA-256 of empty), invalid UTF-8 skipped, binary excluded pre-read AND read-time growth detection, oversized filtered before extraction, nested relative paths, traversal/absolute/null-byte rejection matrix, leaf-symlink escape + symlinked-directory escape fails closed, deterministic lexical ordering across runs, known SHA-256 vector ("hello"), unchanged→same hash / changed→different hash, full filtering integration (.env/node_modules/png/log absent; README/go.mod/main.go/ci.yml present), total-size budget respected, max-file-count respected, no sensitive content in results/errors/logs, empty workspace, cancelled context (filter-phase sentinel vs read-phase sentinel both mapped), unreadable entry fails closed, not-selected + duplicate-path skips. `internal/repositories/extract_test.go`: unauth uniform 401, malformed uuid 400, foreign ≡ unknown opaque 404, missing/corrupted workspace 409, happy path vs a REAL cloned workspace asserting metadata-only body (`"content":` key absent), zero leakage of tmp paths/source markers/env values/symlinked server secrets, node_modules + outside files untouched, byte-identical deterministic responses, log-capture test proving NO source markers and NO absolute workspace paths reach logs even on failure.
  - Verified 2026-08-24: `gofmt -l .` clean; `go build ./...`, `go vet ./...`, `go test ./... -count=1`, `go test -race ./... -count=1` ALL green across all nine packages.

## Completed (2026-08-24 — Task 3e codebase chunking + structural segmentation)

- **Task 3e — `POST /repositories/:id/chunk` (complete, verified 2026-08-24):** deterministic segmentation of Task 3d extracted files into meaningful, bounded chunks with traceable metadata for the future representation/embedding layers. No AI, no embeddings, NO persistence (no new tables).
  - NEW package `internal/chunking` — a PURE transformation over `extraction.Result`: ZERO filesystem access, so every security property (path validation, traversal/symlink refusal, ownership) is inherited from Tasks 3a–3d by construction. `repositories.Service.ChunkRepositoryContent` REUSES `ExtractRepositoryContent` wholesale and attaches the repository identity (the chunker is deliberately repository-agnostic).
  - CHUNK MODEL: `Chunk{RepositoryID, FilePath, Language, ChunkIndex (0-based per file), Content (EXACT substring spanning [StartLine,EndLine], 1-based inclusive; CRLF/BOM preserved), ByteSize, ContentHash (hex SHA-256 of exact bytes), SymbolName/SymbolType/ParentSymbol (BEST-EFFORT, empty when unsure)}`. Concatenating a file's chunks reproduces the extracted content byte-for-byte (property-tested). Hashes change only when content changes.
  - STRUCTURAL STRATEGY (heuristic column-0 boundary detection, deliberately NOT an AST framework): Go (`package`/`import`/`func`(method w/ receiver→ParentSymbol)/`type`/`const`/`var`), Python (`def`/`async def`/`class`, top-level `import`/`from`), JavaScript+TypeScript (`import`, `export [default] function|class`, `function`, `class`), Java (`class`/`interface`/`enum`/`record` granularity only). Doc comments directly above a boundary travel WITH it; consecutive import lines merge into ONE unit; blank separators stay with the preceding unit. Unsupported languages → deterministic line-window fallback that never splits mid-line (a single line longer than the byte cap forms one oversize chunk — documented exception).
  - LIMITS (all env-free, package defaults respect the 3c/3d envelope): MaxChunkLines 200, MaxChunkBytes 16 KiB, MaxChunksPerFile 500, MaxChunksTotal 20000, MaxTotalChunkBytes 32 MiB. Overflow policy is deterministic and flagged: per-file cap → `file_chunk_limit` event + per-file `truncated`; global caps → `repository_chunk_limit`/`repository_byte_limit` events + result `truncated:true` (remaining files skipped safely). Empty files produce zero chunks (`empty_files` count).
  - NO OVERLAP (documented decision): precise start/end line metadata lets consumers re-read context deterministically; overlap would duplicate source and complicate hash-based change detection for no current benefit.
  - API BOUNDARY: response carries aggregates + per-file summaries ONLY (`path/language/chunks/bytes/truncated`, capped at 1000 entries with `files_truncated`) — there is NO content field, NO hashes/line-ranges/symbols crossing HTTP; full chunk detail stays internal.
  - SECURITY: ownership FIRST inherited (unknown ≡ foreign → uniform 404) → readiness gate (409 `workspace_not_ready`) → bounded extract → pure chunking; contents never logged (counts/bytes/duration/IDs only); ignored dirs/symlink targets untouched on disk; absolute paths impossible in responses; error surface identical to extract (504 `extract_timeout`).
  - Tests: `internal/chunking/chunking_test.go` covers ALL 18 required cases (simple/multiline/small/empty files; Go function/method boundaries incl. doc-comment attachment + merged import block; Python class/function; TS export class/function; Java class/interface; CSS fallback windows; large file; deterministic ordering/hashes across runs incl. DeepEqual; SHA-256 verified against direct digest; unchanged-same vs changed-different hashes; contiguous line-number coverage; sequential per-file indexes; byte-cap split at line boundaries; single oversize line; per-file chunk cap vs global caps distinction; global count + byte budgets stop safely; excluded/binary/unicode/CRLF handling; comments preserved verbatim; concat-reproduces-content property; RepositoryID left to caller). `internal/repositories/chunk_test.go`: unauth uniform 401 / malformed uuid 400 / foreign ≡ unknown opaque 404 / missing+corrupted workspace 409 / happy path on a REAL cloned workspace asserting counts (excluded .env/node_modules/png/symlink never enter), structural chunk counts visible per-file, metadata-only body (`"content"` absent), zero leakage of markers/secrets/tmp paths/excluded filenames, node_modules + outside secrets untouched, byte-identical determinism.
  - Verified 2026-08-24: `gofmt -l .` clean; `go build ./...`, `go vet ./...`, `go test ./... -count=1`, `go test -race ./... -count=1` ALL green across all TEN packages.

## Next

- **Task 3f — code representation & metadata:** transform Task 3e chunks into a richer traceable representation (file roles source/test/config/docs, deterministic identity derived from repository_id+path+chunk_index+content_hash, neighbor relationships) — still no AI, still internal-only.

## Future

- skills
- recommendations
- embeddings
- semantic analysis
- Ollama integration
- background workers
- frontend repository UI
